export type Subject = {
    slug: string;
    name: string;
    summary: string;
    guides_total: number;
    paths_total: number
    contributors_total: number;
    languages_total: number;
}
export type SubjectReference = {
    slug: string;
    name: string;
}