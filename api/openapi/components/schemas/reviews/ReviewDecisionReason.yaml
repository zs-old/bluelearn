type: string
description: A rubric reason cited on a rejected decision (at least one required).
enum:
  - hierarchy_issue
  - factual_error
  - duplicate_content
  - scope_violation
  - clarity_issue
  - missing_required_information
example: missing_required_information
