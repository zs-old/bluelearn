type: object
properties:
  id:
    $ref: '../common/Uuid.yaml'
  slug:
    type: string
    example: backend-engineer
  title:
    type: string
    example: Backend Engineer
  summary:
    type: [string, 'null']
  created_at:
    $ref: '../common/Timestamp.yaml'
required: [id, slug, title]
