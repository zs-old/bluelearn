type: object
description: A guide carrying this subject tag.
properties:
  id:
    $ref: '../common/Uuid.yaml'
  slug:
    type: string
    example: calculus
  title:
    type: string
    example: Calculus
  summary:
    type: [string, 'null']
required: [id, slug, title]
