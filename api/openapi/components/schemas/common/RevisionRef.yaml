type: object
description: Lightweight reference to a revision for diff/compare headers.
properties:
  id:
    $ref: './Uuid.yaml'
  author_id:
    $ref: './Uuid.yaml'
  created_at:
    $ref: './Timestamp.yaml'
  change_summary:
    type: [string, 'null']
    example: Fixed the limit definition.
required: [id]
